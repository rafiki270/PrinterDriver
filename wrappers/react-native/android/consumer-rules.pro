# This module has no Java or Kotlin classes -- it is a C++ TurboModule and a TypeScript
# API. There is nothing for R8 to strip and nothing to keep, so this file exists only so
# that `consumerProguardFiles` in build.gradle points somewhere real, and so that a later
# addition (a permissions helper, say) has an obvious home.
